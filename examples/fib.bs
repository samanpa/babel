fn fib(n: i32) -> i32 {
    if int_lt(n, 2) {
        1
    }
    else {
        int_add(fib(int_sub(n, 1))
                , fib(int_sub(n, 2)))
    }
}

fn fib_print(n: i32, print: int -> () ) {
    print(fib(n))
}

fn main() -> int {
    fib_print(10, int_print);
    0
}
