fn id(x) {
    x
}

fn foo(f, x) {
    let id1 = id;
    let id2 = id1;
    let x1  = id1(x);
    f(x1)
}

fn poly(n) {
    let a = id(n);
    let b = foo(id, true);
    b
}

fn max(n, m) {
    let n1 = poly(n);
    let lt = i32_lt;
    if lt(n, m) { 
        n
    } else {
        m
    }
}

fn main() {
    max(10, 4)
}
